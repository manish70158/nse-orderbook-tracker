# DISABLED - V1 workflow replaced by daily-scraper-v2.yml
# This workflow has been disabled in favor of the new V2 API-based scraper
# To re-enable, remove this comment block and the "if: false" condition below

name: Daily Order Book Check (V1 - DISABLED)

on:
  # Disabled by default
  workflow_dispatch:

jobs:
  check-orders:
    runs-on: ubuntu-latest
    if: false  # Disabled - use V2 scraper instead

    steps:
      - name: Disabled
        run: |
          echo "This V1 workflow is disabled. Use daily-scraper-v2.yml instead."
          exit 1
